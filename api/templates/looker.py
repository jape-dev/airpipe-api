"""Module to load looker studio templates"""


def create_view(template_id: int):
    """Creates something new."""
    # Iterates through the table and loads each of the fields into the table.

    # Saves data sources to the db.

    # Includes any custom field calculations

    # Creates blend and join conditions

def load_looker_template():

    #  Redirects to Looker studio with the template loaded.
    pass